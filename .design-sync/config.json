{
  "projectId": "53daa9fc-5618-4769-9cff-7fbf338169de",
  "shape": "package",
  "pkg": "inv-app-web",
  "globalName": "InvAppWeb",
  "srcDir": "src",
  "cssEntry": "src/styles.css",
  "readmeHeader": ".design-sync/conventions.md",
  "dtsPropsFor": {
    "InvitationPreview": "copy: { title: string; greeting: string; body: string; details_line: string; rsvp_prompt: string; closing: string }; design: { palette: 'warm' | 'elegant' | 'playful' | 'minimal' | 'festive' | 'romantic'; typography: 'serif' | 'sans' | 'script'; layout: 'classic' | 'banner' | 'split'; ornament: 'none' | 'floral' | 'geometric' | 'sparkle' }; /** Optional AI background layer (adr-009): an opaque server-issued asset id the card resolves to /api/backgrounds/<id>. Ignored for the minimal palette. Omit it in designs — there is no server to serve the asset. */ background?: { id: string } | null; activeField?: 'title' | 'greeting' | 'body' | 'details_line' | 'rsvp_prompt' | 'closing' | null; onFieldClick?: (field: 'title' | 'greeting' | 'body' | 'details_line' | 'rsvp_prompt' | 'closing') => void"
  },
  "componentSrcMap": {
    "InvitationPreview": "src/components/InvitationPreview.tsx",
    "App": null,
    "GuestPage": null
  }
}
