# Copy to server/.env — the Anthropic SDK reads ANTHROPIC_API_KEY from the environment.
ANTHROPIC_API_KEY=sk-ant-...

# Later, to route through a LiteLLM Proxy instead of Anthropic directly:
# LLM_BASE_URL=http://localhost:4000/anthropic
# PORT=3001
